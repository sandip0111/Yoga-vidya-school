import { ObfuscateEmailPipePipe } from './obfuscate-email-pipe.pipe';

describe('ObfuscateEmailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ObfuscateEmailPipePipe();
    expect(pipe).toBeTruthy();
  });
});
